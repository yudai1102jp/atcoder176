class convett():
    def __init__(self):
        self. = []

    def append(key, value):
        if key in self.dic.keys():
            self.dic[key].append(value)
        else:
            self.dic[key] = [value]

    def sort():
        if
